#pragma once
#include <string> 
#include <cmath> 
#include <vector> 
#include <array> 

namespace Admin
{
    class Admin
    {
         public:
            
            void onboardCoach(Coach c);
            void removeCoach(Coach c);
            void manageAPIKeys();
            void manageAPIKeys(); 
            void triggerDatabaseBackup(); 
    }
}